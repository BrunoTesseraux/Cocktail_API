import { createContext } from "react";

// Gin
export const GinContext = createContext([]);

// Vodka
export const VodkaContext = createContext([]);

// Rum
export const RumContext = createContext([]);

// Alkoholfrei
export const AlcFreeContext = createContext([]);

// Random
export const RandomCocktailContext = createContext([]);

// All ingredients
export const AllIngredientsContext = createContext([]);

// Local storage
export const FavListContext = createContext([]);
